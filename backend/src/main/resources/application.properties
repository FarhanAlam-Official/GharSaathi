spring.application.name=backend

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3307/gharsaathi_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
# IMPORTANT: Use environment variables for production (never commit real secrets)
# Generate a secure key using: openssl rand -base64 64
jwt.secret=${JWT_SECRET:7082cee86dfba39cda1108cec1eccdb53080fcb4af596e72acf89b0e481125ba}
jwt.expiration=${JWT_EXPIRATION:3600000}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:604800000}
jwt.issuer=${JWT_ISSUER:gharsaathi}

# Server Configuration
server.port=8080

# Logging
logging.level.com.gharsaathi=DEBUG
logging.level.org.springframework.security=DEBUG
